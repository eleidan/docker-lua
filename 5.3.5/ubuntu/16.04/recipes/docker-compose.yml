version: '2'

volumes:
  bash-history:

services:
  dev:
    image: eleidan/lua:5.3.5-ubuntu-16.04
    user: phantom
    volumes:
      - .:/home/phantom/app
      - bash-history:/home/phantom/bash-history
